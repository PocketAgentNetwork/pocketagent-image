# ══════════════════════════════════════════════════════════════
# 📟 POCKETAGENT CLOUD CONTAINER CONFIGURATION
# ══════════════════════════════════════════════════════════════

# ── Container Identity ──
# IMPORTANT: This sets both container_name AND hostname
# Hostname persists across container restarts/rebuilds
# Critical for: SSH keys, license keys, services that fingerprint by hostname
CONTAINER_NAME=pocketagent

# For multi-user deployments, use unique names per user:
# CONTAINER_NAME=pocketagent-user123
# VOLUME_PREFIX=user123

# ── Gateway Configuration ──

# Gateway token (optional - OpenClaw will generate one during onboarding if not set)
# For production: PAN backend generates and injects this
# For testing: Run ./setup.sh to auto-generate, or use: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# Gateway port (default: 18789)
OPENCLAW_GATEWAY_PORT=18789

# Bind address:
#   - "lan" = accessible on local network
#   - "127.0.0.1" = localhost only
#   - "0.0.0.0" = accessible from anywhere (use with firewall!)
OPENCLAW_GATEWAY_BIND=lan

# ── PocketAgent Image & Version ──
# Use official image (default) or your own custom build
# POCKETAGENT_IMAGE=ghcr.io/yourusername/pocketagent-image
POCKETAGENT_VERSION=latest

# ── Model API Keys (Optional - Configure during onboarding) ──
# These can be set here or configured through the web UI during onboarding

# Ollama Cloud (Recommended - No GPU needed)
# Sign up at https://ollama.com and get API key at https://ollama.com/settings/keys
# OLLAMA_API_KEY=

# Other Cloud Providers (Optional)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GOOGLE_API_KEY=

# ── Container Metadata (Optional) ──
# Used by PAN backend for tracking and management
# POCKETAGENT_USER_ID=
# POCKETAGENT_CONTAINER_ID=
# POCKETAGENT_REGION=

# ── Feature Flags (Optional) ──
# ENABLE_WEB_SEARCH=true
# ENABLE_CODE_EXECUTION=true

# ══════════════════════════════════════════════════════════════
# 📝 HOW IT WORKS:
# 
# The container uses --allow-unconfigured flag, which means:
# - Container can start without a pre-configured token
# - OpenClaw will handle token generation during onboarding
# - User completes onboarding through web UI or mobile client
# - All config (including token and API keys) saved to persistent volume
#
# DEPLOYMENT SCENARIOS:
#
# 1. PAN-Managed Cloud (Production):
#    - PAN backend generates token automatically
#    - Injects it when provisioning container
#    - User never sees infrastructure
#
# 2. Self-Hosted (Testing/Advanced):
#    - Run ./setup.sh to generate token
#    - Or leave empty and let OpenClaw generate during onboarding
#    - Complete setup through web UI at http://localhost:18789
#
# 3. Local Node:
#    - Installer handles token generation
#    - User onboards through PocketAgent client
#
# ══════════════════════════════════════════════════════════════
